'use strict';

var _animatable = require('./animatable');

var _animatable2 = _interopRequireDefault(_animatable);

var _dom_elements = require('./utils/dom_elements');

var _dom_elements2 = _interopRequireDefault(_dom_elements);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/* global it, expect, describe */
describe('Animatable', function () {
  it('will export an function', function () {
    expect(_animatable2.default).toBeInstanceOf(Function);
  });

  it('will have a method on the prototype for each element', function () {
    _dom_elements2.default.forEach(function (e) {
      return expect(_animatable2.default[e]).toBeDefined();
    });
  });

  it('will the right displayName', function () {
    _dom_elements2.default.forEach(function (e) {
      return expect(_animatable2.default[e].displayName).toBe('Animatable.' + e);
    });
  });
});