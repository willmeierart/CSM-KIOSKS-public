'use strict';

var _dom_elements = require('./dom_elements');

var _dom_elements2 = _interopRequireDefault(_dom_elements);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('domElements', function () {
  it('will export an array', function () {
    expect(_dom_elements2.default).toBeInstanceOf(Array);
  });
  it('will export an array of strings', function () {
    _dom_elements2.default.forEach(function (s) {
      return expect(s).toEqual(expect.any(String));
    });
  });
}); /* global it, expect, describe */